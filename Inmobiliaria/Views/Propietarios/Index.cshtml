@model IEnumerable<Inmobiliaria.Models.Propietario>

<div class="card shadow-sm p-4 mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Propietarios</h2>
        <p>
            <a asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-user-plus"></i> Nuevo Propietario</a>
        </p>
    </div>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>DNI</th>
                <th>Apellido</th>
                <th>Nombre</th>
                <th>Teléfono</th>
                <th>Email</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in Model)
            {
                <tr>
                    <td>@p.Dni</td>
                    <td>@p.Apellido</td>
                    <td>@p.Nombre</td>
                    <td>@p.Telefono</td>
                    <td>@p.Email</td>
                    <td class="text-nowrap">
                        <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-sm btn-info text-white me-1" title="Ver Detalles">
                            <i class="fa-solid fa-eye"></i> Ver
                        </a>
                        <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-success text-white me-1" title="Editar">
                            <i class="fa-solid fa-pencil"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@p.Id" class="btn btn-sm btn-danger text-white" title="Dar de Baja">
                            <i class="fa-solid fa-trash-can"></i> Dar de Baja
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>